{
  "name": "eidos-script-publish",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "tsc --watch",
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --platform=browser --format=esm --loader:.html=text"
  },
  "devDependencies": {
    "@eidos.space/types": "^0.0.4",
    "esbuild": "^0.19.7",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "marked": "^10.0.0"
  }
}
