{
  "name": "eidos-script-publish",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.ts --watch --bundle --outfile=dist/main.js --platform=browser --format=esm --loader:.html=text",
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --platform=browser --format=esm --loader:.html=text"
  },
  "devDependencies": {
    "@eidos.space/types": "^0.0.7",
    "esbuild": "^0.19.7",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "marked": "^10.0.0"
  }
}